server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback: nếu không tìm thấy file/thư mục vật lý,
        # trả về index.html để React Router xử lý routing
        try_files $uri $uri/ /index.html;
    }
}
